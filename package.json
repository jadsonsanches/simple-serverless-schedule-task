{
  "name": "serverless-scheduled-task",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "serverless offline start --stage local"
  },
  "devDependencies": {
    "serverless-offline": "^8.8.0"
  }
}